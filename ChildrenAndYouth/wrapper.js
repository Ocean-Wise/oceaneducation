import ChildrenAndYouthEl from './ChildrenAndYouthEl';
import ReactDOM from 'react-dom';
import React from 'react';

const ChildrenAndYouth = (element) => {
  ReactDOM.render(<ChildrenAndYouthEl />,
    element);
};

module.exports = ChildrenAndYouth;
