import CurriculumEl from './CurriculumEl';
import ReactDOM from 'react-dom';
import React from 'react';

const Curriculum = (element) => {
  ReactDOM.render(<CurriculumEl />,
    element);
};

module.exports = Curriculum;
