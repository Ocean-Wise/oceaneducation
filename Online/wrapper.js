import OnlineEl from './OnlineEl';
import ReactDOM from 'react-dom';
import React from 'react';

const Online = (element) => {
  ReactDOM.render(<OnlineEl />,
    element);
};

module.exports = Online;
