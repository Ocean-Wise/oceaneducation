import MobileEl from './MobileEl';
import ReactDOM from 'react-dom';
import React from 'react';

const Mobile = (element) => {
  ReactDOM.render(<MobileEl />,
    element);
};

module.exports = Mobile;
